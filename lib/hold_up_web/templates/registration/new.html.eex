<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
  <meta charset="utf-8"/>
  <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>HoldUp</title>
  <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
</head>
<body class="h-100" style="background-color: #f4f2f0;">
  <div class="d-flex flex-column justify-content-center align-items-center h-100">
    <div class="card cardForm">
      <div class="card-body">
        <h5 class="card-title text-center"><%= gettext("Sign Up") %></h5>
        <div class="mt-3">
          <%= render "form.html", Map.put(assigns, :action, Routes.registration_path(@conn, :create)) %>
        </div>
      </div>
      <div class="card-footer text-muted text-center">
        <span><%= gettext("Already have an account?") %></span>
        <%= link gettext("Sign In"), to: Routes.session_path(@conn, :new), class: "text-underline" %>
      </div>
    </div>
  </div>
  <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</body>
</html>
